<div class="modal fade" id="inspirationModalUpdate-<%= inspiration.id %>" tabindex="-1" role="dialog" aria-labelledby="inspirationModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">

      <div class="modal-header">
        <h6 class="modal-title">Update your source of inspiration</h6>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>

      <div class="modal-body">
        <%= simple_form_for [track, inspiration] do |f| %>
          <%= f.input :name %>
          <div class="text-center mt-4">
            <%= f.button :submit, label: 'Update', class: "btn btn-blue" %>
          </div>
        <% end %>
      </div>

    </div>
  </div>
</div>
